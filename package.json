{
  "name": "ngraph.fromdot",
  "version": "6.0.1",
  "description": "Load dot files into ngraph.graph",
  "main": "index.js",
  "scripts": {
    "test": "tap --branches=70 --lines=80 --statements=80 --functions=80 test/*.js",
    "build": "browserify index.js -s fromDot -o dist/ngraph.fromDot.js && uglifyjs dist/ngraph.fromDot.js -o dist/ngraph.fromDot.min.js"
  },
  "keywords": [
    "ngraph",
    "dot",
    "graphviz",
    "graph",
    "serialization"
  ],
  "author": "Andrei Kashcha",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/anvaka/ngraph.fromdot"
  },
  "devDependencies": {
    "tap": "^15.0.9",
    "uglify-js": "^3.13.8"
  },
  "dependencies": {
    "dotparser": "^1.0.0",
    "ngraph.graph": "^19.1.0"
  }
}
